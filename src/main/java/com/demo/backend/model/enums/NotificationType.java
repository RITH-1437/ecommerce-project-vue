package com.demo.backend.model.enums;

/**
 * Enum representing different types of notifications.
 */
public enum NotificationType {
    ORDER, // Order-related notifications
    PRODUCT, // Product updates (restock, price drops)
    PROMOTION, // Promotional notifications (sales, coupons)
    REVIEW, // Review-related notifications
    SYSTEM // System announcements and updates
}
