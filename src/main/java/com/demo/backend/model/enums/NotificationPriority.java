package com.demo.backend.model.enums;

/**
 * Enum representing notification priority levels.
 */
public enum NotificationPriority {
    LOW, // Low priority (nice to know)
    NORMAL, // Normal priority (default)
    HIGH, // High priority (important)
    URGENT // Urgent (critical, requires immediate attention)
}
